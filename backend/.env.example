# Bu dosyayı `.env` olarak kopyalayın ve anahtarınızı girin (backend/ klasörüne):
#   cp .env.example .env
# `.env` .gitignore'dadır; repoya gitmez.

# OpenAI (GPT-4o mini) — boş bırakılırsa AI geri bildirimi klinik tablodan gelir.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
